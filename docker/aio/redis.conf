# Redis configuration for the all-in-one container.
# Bound to localhost only - never exposed to the network.
bind 127.0.0.1
port 6379
protected-mode yes
appendonly yes
appendfsync everysec
dir /app/data/redis
maxmemory-policy noeviction
# Logs go to stdout so supervisord captures them
logfile ""
daemonize no
